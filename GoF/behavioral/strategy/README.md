# Strategy Pattern

## Overview
Strategy is a behavioral design pattern that lets you define a family of algorithms, put each of them into a separate class, and make their objects interchangeable. The original object delegates the execution to one of these strategy objects instead of implementing all behaviors itself.
